/**
 * DeepSeek Harness plugin entry.
 *
 * 0.0.1 is a reserved npm name / installable bundle stub.
 * ctx.web providers and extra extraction tools land in a later release.
 */

export const name = 'dsh-web-access'

export function apply() {
  console.info('[dsh-web-access] placeholder 0.0.1 loaded — search/fetch providers are not implemented yet')
}
